# Base URL of the Server-Monitor backend API.
# Defaults to the deployed instance if unset (see src/api/client.ts).
# Note: the hosted free tier may cold-start (~30–60s) on the first request.
VITE_API_BASE_URL="https://server-monitor-skil.onrender.com"
